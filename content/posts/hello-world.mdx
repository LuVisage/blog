---
title: 'Hello World - 我的第一篇博客'
date: '2026-07-13'
description: '这是我的个人博客的第一篇文章，记录搭建博客的过程和未来的写作计划。'
category: 'Blog'
series: '博客搭建'
tags:
  - Blog
  - AI Agent
  - LLM
---

## 欢迎来到我的博客 🎉

经过一番努力，我的个人博客终于上线了！这篇文章将介绍这个博客的技术栈和我的写作计划。

## 技术栈

这个博客基于以下技术搭建：

- **框架**：Next.js 15 + React 19
- **内容**：MDX - 在 Markdown 中嵌入 React 组件
- **样式**：Tailwind CSS + Typography 插件
- **搜索**：Pagefind - 纯静态全文搜索
- **评论**：Giscus - 基于 GitHub Discussions
- **部署**：GitHub Pages

全部 **静态化**，不需要服务器，不需要数据库。

## 代码示例

作为一个 AI 开发者博客，代码块是必不可少的：

```python
# A simple AI Agent loop
from openai import OpenAI

client = OpenAI()

def agent_loop(prompt: str, tools: list[dict]) -> str:
    messages = [{"role": "user", "content": prompt}]
    while True:
        response = client.chat.completions.create(
            model="gpt-4o",
            messages=messages,
            tools=tools,
        )
        msg = response.choices[0].message
        if msg.tool_calls:
            messages.append(msg)
            for tool_call in msg.tool_calls:
                result = execute_tool(tool_call)
                messages.append({
                    "role": "tool",
                    "tool_call_id": tool_call.id,
                    "content": str(result),
                })
        else:
            return msg.content
```

## 写作计划

我计划在这里写以下内容：

1. **AI Agent 开发** - Agent 架构设计、工具调用、多 Agent 协作实践
2. **LLM 应用实战** - 大模型 API 使用、Prompt Engineering、RAG 系统
3. **项目复盘** - AI 项目从设计到部署的完整思考
4. **工具与框架** - AI 开发工具链、框架评测与推荐

## 关于我

一名专注于 AI Agent 开发的工程师。在这个博客里，我将分享 AI 开发中的实战经验与技术思考，记录构建智能体应用的探索过程。

> "写作是最好的思考方式。"

欢迎通过评论与我交流！
