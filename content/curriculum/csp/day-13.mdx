---
slug: day-13
kind: day
day: 13
week: 2
weekTitle: 进阶算法
weekBlurb: 给 T4 备武器：KMP 与字符串哈希、并查集与最小生成树、最短路拓扑排序、树状数组线段树、线性/背包/区间/树形/状压 DP、搜索与剪枝。
title: 动态规划②——区间 DP / 树形 DP / 状压 DP
lead: DP 类 T4 的正解能力（下）
sourcePath: 第2周-Day08-14-真题实战/Day13-动态规划2-区间树形状压.md
---

## 今日任务

1. 掌握区间 DP（石子合并模型）并默写；
2. 掌握树形 DP（最大独立集模型）与树的直径；
3. 了解状压 DP（TSP 模型）；
4. 做 3 道洛谷 + 1 道真题。

## 学什么

### 1. 区间 DP：从小区间推向大区间
```cpp
// 石子合并：dp[l][r] = 合并 [l,r] 的最小代价
for (int len = 2; len <= n; len++)                 // 枚举区间长度
    for (int l = 1; l + len - 1 <= n; l++) {       // 枚举左端点
        int r = l + len - 1;
        dp[l][r] = 1e18;
        for (int k = l; k < r; k++)                // 枚举分割点
            dp[l][r] = min(dp[l][r],
                dp[l][k] + dp[k+1][r] + sum[r] - sum[l-1]);
    }
// 环上区间 DP：数组复制一倍 a[i+n]=a[i]，枚举起点，取最优
```
**真题：201612-4 压缩编码**——本质是"石子合并"（合并代价=两段频率和），直接套模板。

### 2. 树形 DP：在树上做 DP
```cpp
// 最大独立集（没有上司的舞会）：dp[u][0] 不选 u，dp[u][1] 选 u
void dfs(int u, int fa) {
    dp[u][1] = a[u]; dp[u][0] = 0;
    for (int v : g[u]) if (v != fa) {
        dfs(v, u);
        dp[u][0] += max(dp[v][0], dp[v][1]);
        dp[u][1] += dp[v][0];
    }
}
// 树的直径：两遍 BFS/DFS（任一点找最远点 a，再从 a 找最远点 b，ab 即直径）
// 真题：201503-4 网络延时 = 树的直径
```

### 3. 状压 DP：集合状态压缩成二进制
```cpp
// TSP：dp[mask][i] = 已访问集合 mask、当前在 i 的最小代价
for (int mask = 1; mask < (1 << n); mask++)
    for (int i = 0; i < n; i++) if (mask >> i & 1)
        for (int j = 0; j < n; j++) if (!(mask >> j & 1))
            dp[mask | 1 << j][j] =
                min(dp[mask | 1 << j][j], dp[mask][i] + dis[i][j]);
// n 一般 ≤ 20；初始化 dp[1<<i][i] = 0
```
**真题参考：202109-4 收集卡牌**（状压 DP + 期望，先只做子任务）、202012-4 食材运输（状压 + 树形，很难，了解即可）。

## 练什么

**洛谷（每题 ≤ 45 分钟）：**

| 题号 | 考点 |
|---|---|
| [P1775](https://www.luogu.com.cn/problem/P1775) 石子合并（弱化版） | 区间 DP |
| [P1352](https://www.luogu.com.cn/problem/P1352) 没有上司的舞会 | 树形 DP |
| [P1433](https://www.luogu.com.cn/problem/P1433) 吃奶酪 | 状压 DP（TSP） |

**真题（限时 60 分钟）：**

| 题号 | 考点 |
|---|---|
| 201612-4 压缩编码 | 区间 DP（石子合并变体） |

## 今日模板

完整版本见[代码模板速查手册](/learn/csp/templates)（第 22、23、24 节）。

```cpp
// 区间 DP 三连循环：len → l → k（背住顺序！）
// 树形 DP：dfs 后自底向上合并
// 状压 DP：mask 从 1 到 (1<<n)-1，枚举当前点 i 与目标点 j
```

## 验收标准

<Quiz items={["闭卷默写区间 DP（三连循环）与树形 DP（dfs 合并）","P1775、P1352 AC；201612-4 压缩编码 AC（这是第二道 T4 满分题！）","能说出状压 DP 的适用条件（n ≤ 20 且状态可枚举）。"]} />

