# 本文件被 git 跟踪、会随仓库公开：只允许放 NEXT_PUBLIC_* 前缀的公开构建配置。
# 真正的秘密（密钥、令牌）一律不放这里 —— Worker 侧走 Cloudflare 的
# wrangler secret / 控制台变量，CI 侧走 GitHub Secrets。
# scripts/audit-build.mjs 会校验这里的每一行都必须以 NEXT_PUBLIC_ 开头。
NEXT_PUBLIC_BASE_PATH=/blog
